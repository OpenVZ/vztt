# upgrade
#CE_SERVER=http://mirror.centos.org
CFG_FILE=/etc/vztt/vztt.conf
URL_MAP_FILE=/etc/vztt/url.map
[ -f $URL_MAP_FILE ] && exit 0
if [ -f $CFG_FILE ] ; then
	if ! grep -q "CE_SERVER=" $CFG_FILE ; then
		echo -e "\nCE_SERVER=http://mirror.centos.org\n" >> $CFG_FILE
	fi
fi
:
